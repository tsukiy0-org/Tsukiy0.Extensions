import { Construct } from "constructs";
import { External } from "./External";
export declare class ExampleAspNetCoreApi extends Construct {
    constructor(scope: Construct, id: string, props: {
        external: External;
    });
}
